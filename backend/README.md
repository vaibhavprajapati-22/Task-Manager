# Backend (TasksApi)

Requirements: .NET 8 SDK installed.

Run:
```bash
cd backend
dotnet restore
dotnet run
```

API endpoints:
- GET /api/tasks
- POST /api/tasks
- PUT /api/tasks/{id}
- DELETE /api/tasks/{id}
